# Copia este archivo a secrets.toml para probar localmente (secrets.toml
# esta en .gitignore, nunca se sube al repo publico).
#
# En Streamlit Community Cloud, esto se configura desde el panel web de la
# app (Settings -> Secrets), NO se sube como archivo.

APP_PASSWORD = "cambia-esta-clave"
